/**
 * Coredeux export module entry package.
 *
 * <p>The module queues export requests, executes them asynchronously in pages,
 * stores the output through a pluggable storage backend, and exposes job
 * status and logs by uid.</p>
 */
package com.coredeux.export;
