# Coredeux Export

This module contains the plain Java export pipeline for Coredeux.

It includes the queue, log, storage, writer, resolver, service, and worker implementations without any Spring annotations. Spring Boot applications should use `coredeux-export-spring-boot-starter` for auto-configuration.
