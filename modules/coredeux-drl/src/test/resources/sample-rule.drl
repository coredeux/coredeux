package rules.sample;

import com.coredeux.drl.model.RuleContext;

rule "sample-rule"
when
    $context : RuleContext(method == "sample-rule")
then
    $context.setOutput("classpath-default");
    $context.setMessage("classpath-default");
end
