/**
 * Runtime support for executing Coredeux rules through Drools.
 *
 * <p>This module owns the rule execution engine, cache, source resolution, and
 * runtime bootstrap behavior for compiled DRL. Public types in this package are
 * intended for application runtime use, while authoring-time conversion tools
 * live in the separate {@code coredeux-drl-devtools} module.
 */
package com.coredeux.drl;
