# Coredeux Export Spring Boot Starter

This module provides the Spring Boot wiring for `coredeux-export`.

It registers the default export queue, log, storage, writer, resolver, service, and worker beans so Spring applications can consume the export pipeline without manually assembling the core Java classes.
